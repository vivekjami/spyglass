# Benchmark

**Status:** methodology only. **No results exist yet** — every number below is
`[MEASURE AFTER IMPLEMENTATION]`. Result tables are generated by
`bench/report.py` from committed run files and are **never hand-edited**.

## The controlled comparison

Two conditions, identical in **model, harness, incident, information access, and
action path**, differing only in the evidence interface:

- **BASELINE** — raw telemetry tools (`tail_logs`, `grep_logs`, `get_metric`,
  `list_services`, `deploy_events`) → gated `rollback`.
- **SPYGLASS** — the evidence plane (bounded, ranked, novelty/changepoint-aware
  tools + bundles) → gated `rollback`.
- **ABLATION A1** — Spyglass with `novel_templates` disabled, isolating the
  headline tool's contribution. Implemented as a `disable_tools` entry, not a
  code change.

## Conditions as built

Each condition is a TrueForge agent manifest in `bench/conditions/`, registered
by name with `just tf-setup`. `bench/conditions/README.md` carries the fairness
checklist and the information-access mapping between raw and shaped tools.

| Condition | Agent | Read tools | Action path | Status |
|---|---|---|---|---|
| BASELINE | `spyglass-baseline` | `spyglass-rawtools`: `list_services`, `tail_logs`, `grep_logs`, `get_metric`, `deploy_events`, `http_request` (one request, like `curl` — the raw counterpart of the replay, Phase 8) | `spyglass-deployer`: `rollback` (approval-required), `current_versions` | built (Phase 2; `http_request` Phase 8) |
| SPYGLASS | `spyglass` | the evidence plane: `build_evidence_bundle` (the starter), `novel_templates`, `detect_changepoints`, `error_delta`, `deploy_events`, `freshness_watermark`, `get_evidence`, `search_logs`, `service_topology`, and the causal check `get_exemplar_request` → `replay_exemplar` | the same deployer server, same gate | built (Phases 3–8) |
| ABLATION A1 | `spyglass-no-novelty` | Spyglass with `disable_tools: ["novel_templates"]` | same | built (Phase 4) — one line in the condition file |

One investigation = `scripts/investigate.py --condition <c> --scenario <s>`:
it opens a session on the named agent, posts the alert, drives the approval
gate per policy (`ask` for filmed runs, `allow`/`deny` unattended), and writes
`bench/results/<s>-<c>-<run>.json` with the metrics below plus the full event
trace, so every number is traceable to what happened.

## Pinned harness settings (ADR-016)

Every condition file states these explicitly rather than inheriting defaults,
because TrueForge's defaults would otherwise shape the baseline's tool output
and contaminate the control group:

```json
"config": {
  "iteration_limit": <pinned>,
  "context_management": {
    "compaction":          {"enabled": false},
    "large_tool_response": {"enabled": false}
  }
}
```

Plus, on every MCP server entry, `"preload": true` — deferred tool loading
otherwise adds harness-side `list_tools`/`get_tool_info` calls to the tool-call
metric, unevenly across conditions.

Rationale and reversal conditions: [ADR-016](adr/ADR-016-harness-context-management-pinning.md).

## Metrics

Investigation success · root-cause accuracy (top-1) · evidence precision ·
evidence recall · tool calls · input/output/total tokens · estimated cost
(provider price sheet committed with results) · investigation latency (RCA, and
separately verified-close including human approval latency) · time to first
useful hypothesis · false hypotheses · verification success.

Engine-side, per condition: tool-call latency P50/P95/P99, bundle reduction
ratio, bytes served to context.

**How tokens are counted.** A turn's `state.output.usage` is the usage of its
*final* model call only. Metrics 6–8 are therefore computed by summing
`usage` over every `model.message` event in the turn's event stream, across
the main thread and every sub-agent thread (`scripts/tf.py::usage_total`).
Measured in Phase 0: a turn with one trivial sub-agent was 2745/197 tokens
by the sum and 1340/30 by the turn-level field.

## Preliminary single runs (n=1 — not results)

Recorded here so the numbers exist before the treatment does and cannot be
tuned toward afterwards. **n=1–2, one model, one scenario; no claim rests on
this table.** On S1 the treatment matches the baseline on correctness, cuts
tool bytes and peak context, cites every claim; Phase 3's engine did **not**
cut input tokens (F6 explains why), Phase 4's `novel_templates` cut them 28%
below the baseline (Phase 4 F4); Phase 5's `detect_changepoints` added evidence and tokens on S1 (P5 F6 — S2 is where it should pay); Phase 7's bundle put both back under the baseline (177.7k / 184.3k) with every eid cited; Phase 8's causal check adds two tool calls, two model calls and ~1 s, and the RCA's label changes from *correlational* to *causal, measured* — total input tokens rise to 243k / 260k, of which 162k are cache reads (uncached input 80.5k / 98.2k vs 78.9k in P7). **Tokens are reported next to the RCA's label from here on** (P8 F6): a causal RCA is a different deliverable from a correlational one. Phase 9 adds the proposal step and the engine-judged verification (two more tool calls; verification success is now read from the ledger's `verified_recovery` / `escalation` entry, never from the prose) and a **deny-path run**: the approval refused, the agent ends report-only with zero further calls. n=1 per row. The real table is generated by `report.py` in Phase 10.

| Scenario | Condition | Outcome | RCA correct | Tool calls | Model calls | Input tokens | Output tokens | Tool bytes → context | Wall | Run file |
|---|---|---|---|---|---|---|---|---|---|---|
| S1 | spyglass (P9 run 3: SOP v6, check spacing enforced — the record run) | completed; replay 0/20 vs 20/20; `propose_rollback` → `rollback` `D-3` citing E1/E2/E3/E6/E7/E8; **engine CLOSED** after 2 consecutive clean checks ≥ 15 s apart (4 checks incl. one `too_soon`); 20.6% → 0.0%; 11/12 eids cited; re-check PASS 2/2 | yes, **CAUSAL** | 14 | 15 | 318,993 | 5,624 | 43,638 | 74.6 s | `s1-spyglass-20260829T105630Z.json` |
| S1 | spyglass (P9 run 2: SOP v6 — propose → gated rollback → engine-judged verification) | completed; replay 0/20 vs 20/20; `propose_rollback` → `rollback` `D-3` citing E1/E2/E3/E7/E8/E9; **engine CLOSED** after 2 consecutive clean checks (3 checks); 20.6% → 0.0%; **12/12 eids cited**; re-check PASS 2/2 | yes, **CAUSAL** | 12 | 12 | 251,009 (178,621 cache) | 5,555 | 38,386 | 55.5 s | `s1-spyglass-20260829T104208Z.json` |
| S1 | spyglass (P9 run 1: SOP v6) | completed; replay 0/20 vs 20/20; `D-3` citing E1/E2/E3/E8/E9/E10; **engine CLOSED** (3 checks); 20.6% → 0.0%; **13/13 eids cited**; re-check PASS 2/2 | yes, **CAUSAL** | 12 | 13 | 267,159 (186,756 cache) | 7,344 | 38,313 | 63.5 s | `s1-spyglass-20260829T103650Z.json` |
| S1 | spyglass (P9 **deny** run: approval refused at the gate) | completed **report-only**: proposal recorded, rollback denied ("Operator declined"), **0 further tool calls**, no retry, fault left active by design (20.8% → 24.7%); 8/8 eids cited; re-check PASS 2/2 | yes, **CAUSAL**, action withheld | 8 | 8 | 145,914 (105,528 cache) | 5,219 | 25,464 | 35.3 s | `s1-spyglass-20260829T104656Z.json` |
| S1 | spyglass (P8 run 2: SOP v5, causal check) | completed; replay v1 0/20 vs v2 20/20 `separated`; rollback `D-3` citing E1/E2/E3/E7/E8/E9; 20.6% → 0.0%; **15/15 eids cited**; re-check PASS 4/4 | yes, **labelled CAUSAL** (one-class limit stated) | 12 | 13 | 260,172 (161,963 cache) | 8,424 | 34,333 | 77.6 s | `s1-spyglass-20260829T100758Z.json` |
| S1 | spyglass (P8 run 1: SOP v5, causal check) | completed; replay v1 0/20 vs v2 20/20 `separated`; rollback `D-3` citing E1/E2/E3/E6/E7/E8; 26.3% → 0.0%; 13/14 eids cited (the INFO decoy uncited); re-check PASS 4/4 | yes, **labelled CAUSAL** | 12 | 12 | 242,957 (162,426 cache) | 6,234 | 32,929 | 60.5 s | `s1-spyglass-20260829T100250Z.json` |
| S1 | baseline | completed; rollback `D-3`; 20.6% → 0.0% | yes | 19 | 11 | 198,106 | 4,978 | 57,147 | 39.5 s | `s1-baseline-20260829T021835Z.json` |
| S1 | baseline | harness_error (schema, F8) | — | 0 | 0 | 0 | 0 | 0 | 1.1 s | `s1-baseline-20260829T021502Z.json` |
| S1 | spyglass | completed; rollback `D-3` citing E1/E5/E7; 19.8% → 0.0%; 15/15 eids cited; re-check 1 mismatch (P3 F6) | yes, labelled correlational | 14 | 15 | 201,297 | 8,389 | 31,020 | 69.6 s | `s1-spyglass-20260829T032658Z.json` |
| S1 | spyglass | completed; rollback `D-3` citing 7 eids; 20.8% → 0.0%; 15/18 eids cited; **re-check PASS 7/7** | yes, labelled correlational | 15 | 16 | 228,628 | 6,185 | 32,136 | 63.5 s | `s1-spyglass-20260829T033353Z.json` |
| S1 | spyglass (P4: `novel_templates` + SOP v2) | completed; rollback `D-3` citing E3/E4; 20.6% → 0.0%; **14/14 eids cited**; re-check PASS 4/4 | yes, labelled correlational | **12** | **9** | **141,601** | 7,551 | 31,363 | 70.5 s | `s1-spyglass-20260829T041208Z.json` |
| S1 | spyglass (P5 run 1: `detect_changepoints` v1, 16 kB response) | completed; rollback `D-3` citing E2/E3/E8/E11/E16; 20.6% → 0.0%; 21/23 eids cited; timeline with deploy offsets; **re-check FAIL 4/5** (rollback joined on replay — fixed, P5 F6) | yes, labelled correlational (+1.4 s offset stated) | 13 | 11 | 259,677 | 8,827 | 47,148 | 82.6 s | `s1-spyglass-20260829T074858Z.json` |
| S1 | spyglass (P7 run 2: SOP v4, bundle-first, safe watermark) | completed; rollback `D-3` citing E1/E2/E3; 20.6% → 0.0%; **11/11 eids cited**; cascade + offset in the timeline; **re-check PASS 3/3** | yes, labelled correlational (+0.6 s) | **11** | 12 | **184,309** | 6,421 | **26,536** | 71.5 s | `s1-spyglass-20260829T092854Z.json` |
| S1 | spyglass (P7 run 1: SOP v4, bundle-first) | completed; rollback `D-3` citing E1/E2/E3; 20.6% → 0.0%; 11/11 eids cited; re-check FAIL 2/3 (one late event; P7 F6) | yes, labelled correlational (+0.6 s) | 11 | 10 | 177,672 | 7,527 | 30,288 | 84.6 s | `s1-spyglass-20260829T092011Z.json` |
| S1 | spyglass (P5 run 2: compact `detect_changepoints`, 6.3 kB) | completed; rollback `D-3` citing E2/E3/E9/E10; 20.6% → 0.0%; 16/18 eids cited; changepoint offsets in the timeline; **re-check PASS 5/5** | yes, labelled correlational (+0.6 s offset stated) | 13 | 11 | 222,647 | 8,186 | 38,133 | 92.7 s | `s1-spyglass-20260829T080531Z.json` |

## Statistical honesty

n=3 repeats per cell is a hackathon budget, not a study. Therefore:

- Report per-run values and ranges, not just means.
- **Claim no statistical significance** — at n=3 none is justified.
- Treat model nondeterminism as irreducible noise; let disagreement across
  repeats stay visible.
- State the bias plainly: scenario authorship and system authorship are the same
  person. Mitigated — not eliminated — by pinned seeds, ground truth committed
  *before* benchmark runs, and raw run files in the repo.
- Cherry-picking is structurally prevented: **every** run is committed to
  `bench/results/`, including failures.
